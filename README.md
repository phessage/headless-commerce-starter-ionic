# 1Ecomm Ionic + Capacitor Storefront Starter

Hybrid web/iOS/Android reference storefront using Ionic React and the `/v1/headless/products` preview contract. Run `npm install && npm run check`.

The default catalog is synthetic and contains no production data. Native packaging requires the documented mobile authentication/deep-link gate before release.
