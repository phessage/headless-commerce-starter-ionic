import{expect,test}from'@playwright/test';test('loads catalog on a mobile viewport and updates cart',async({page})=>{const response=page.waitForResponse(r=>r.url().endsWith('/products.json'));await page.goto('/');expect((await(await response).json()).requestId).toBe('synthetic_ionic');await expect(page.getByRole('heading',{name:'Summit Bottle'})).toBeVisible();await page.getByRole('button',{name:'Add Summit Bottle to cart'}).click();await expect(page.getByText('Cart 1')).toBeVisible();await expect(page.getByRole('button',{name:'Add Storm Tent to cart'})).toBeDisabled()});
